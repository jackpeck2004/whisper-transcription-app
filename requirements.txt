# Cross-platform (Apple Silicon uses Metal/MPS; no CUDA wheels on Mac).
# Pin only app + whisper; pip/uv resolves torch, numpy, etc.
# Use Python 3.10–3.14 with uv: `uv venv && uv pip install -r requirements.txt`
Flask==3.0.3
Flask-SocketIO==5.3.7
openai-whisper==20250625
pydub==0.25.1
Werkzeug==3.0.4
